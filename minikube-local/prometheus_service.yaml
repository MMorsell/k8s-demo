apiVersion: v1
kind: Service
metadata:
  name: my-nodeport-service
spec:
  type: NodePort
  selector:
    app: kube-prometheus-stack-prometheus
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9090
      nodePort: 30080
